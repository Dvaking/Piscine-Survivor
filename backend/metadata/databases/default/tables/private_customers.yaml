table:
  name: customers
  schema: private
object_relationships:
  - name: encounters
    using:
      manual_configuration:
        column_mapping:
          id: customer_id
        insertion_order: null
        remote_table:
          name: encounters
          schema: private
  - name: user_auth
    using:
      manual_configuration:
        column_mapping:
          uuid: customer_uuid
        insertion_order: null
        remote_table:
          name: users
          schema: public
