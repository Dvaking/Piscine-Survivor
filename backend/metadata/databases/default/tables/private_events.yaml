table:
  name: events
  schema: private
array_relationships:
  - name: eventCustomers
    using:
      manual_configuration:
        column_mapping:
          uuid: event_uuid
        insertion_order: null
        remote_table:
          name: eventCustomers
          schema: private