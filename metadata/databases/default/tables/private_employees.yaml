table:
  name: employees
  schema: private
object_relationships:
  - name: user_auth
    using:
      manual_configuration:
        column_mapping:
          id: employee_uuid
        insertion_order: null
        remote_table:
          name: users
          schema: public
